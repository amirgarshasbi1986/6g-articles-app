<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Podcasts - 6G Workgroup MCI</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="{{ url_for('static', filename='app.css') }}">
</head>
<body>
  <!-- Banner (same as dashboard) -->
  <div class="banner-container">
    <img src="{{ url_for('static', filename='banner.jpg') }}" alt="6G Workgroup MCI Banner" class="banner-image">
    <div class="banner-overlay">
      <!-- Title at bottom left -->
      <div class="banner-title-left">
        <h1>6G Workgroup MCI</h1>
      </div>
      <!-- Navigation at bottom right -->
      <nav class="banner-nav-right">
        <ul class="nav-list">
          <li><a href="/">Home</a></li>
          <li><a href="/articles">Articles</a></li>
          <li><a href="/videos">Videos</a></li>
          <li><a href="/podcasts" class="active">Podcasts</a></li>
          <li><a href="/about">About</a></li>
        </ul>
      </nav>
    </div>
  </div>

  <div class="container mt-4">
    <div class="row g-3"> <!-- Reduced gutter for less gap -->
      <div class="col-md-3 sidebar mb-3 mb-md-0"> <!-- Reduced margin-bottom on mobile -->
        <h3>Select Week</h3>
        {% if weeks %}
          <form method="get">
            <select name="week" class="form-select" onchange="this.form.submit()"> <!-- Removed mb-4 for no gap -->
              {% for week in weeks %}
                <option value="{{ week }}" {% if week == selected_week %}selected{% endif %}>{{ week }}</option>
              {% endfor %}
            </select>
          </form>
        {% else %}
          <p>No weeks available</p>
        {% endif %}
      </div>
      <div class="col-md-9">
        <h1 class="mt-3 mt-md-0">Podcasts for Week {{ selected_week }}</h1> <!-- Conditional margin: add on mobile, remove on desktop -->
        {% if podcasts %}
          <div class="row">
            {% for podcast in podcasts %}
              <div class="col-md-6 mb-3">
                <div class="card h-100">
                  <div class="card-body d-flex flex-column">
                    <h5 class="card-title">{{ podcast.title }}</h5>
                    <p class="card-text">{{ podcast.summary_short }}</p>
                    <audio controls class="w-100">
                      <source src="/podcasts/{{ media_week }}/{{ podcast.path }}" type="audio/mpeg">
                      Your browser does not support the audio tag.
                    </audio>
                    <div class="mt-auto text-end">
                      <a href="/podcasts/{{ selected_week }}/{{ podcast.id }}" class="btn btn-primary">Read Article</a>
                    </div>
                  </div>
                </div>
              </div>
            {% endfor %}
          </div>
        {% else %}
          <p>No podcasts available for week {{ selected_week }}</p>
        {% endif %}
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
