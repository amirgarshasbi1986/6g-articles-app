<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Articles - 6G Workgroup MCI</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="{{ url_for('static', filename='app.css') }}">
</head>
<body>
  <!-- Banner (same as dashboard) -->
  <div class="banner-container">
    <img src="{{ url_for('static', filename='banner.jpg') }}" alt="6G Workgroup MCI Banner" class="banner-image">
    <div class="banner-overlay">
      <!-- Title at bottom left -->
      <div class="banner-title-left">
        <h1>6G Workgroup MCI</h1>
      </div>
      <!-- Navigation at bottom right -->
      <nav class="banner-nav-right">
        <ul class="nav-list">
          <li><a href="/">Home</a></li>
          <li><a href="/articles" class="active">Articles</a></li>
          <li><a href="/videos">Videos</a></li>
          <li><a href="/podcasts">Podcasts</a></li>
          <li><a href="/about">About</a></li>
        </ul>
      </nav>
    </div>
  </div>

  <div class="container mt-4">
    <div class="row g-3"> <!-- Reduced gutter for less gap -->
      <div class="col-md-3 sidebar mb-3 mb-md-0"> <!-- Reduced margin-bottom on mobile -->
        <h3>Select Week</h3>
        {% if weeks %}
          <form method="get">
            <select name="week" class="form-select" onchange="this.form.submit()"> <!-- Removed mb-4 for no gap -->
              {% for week in weeks %}
                <option value="{{ week }}" {% if week == selected_week %}selected{% endif %}>{{ week }}</option>
              {% endfor %}
            </select>
          </form>
        {% else %}
          <p>No weeks available</p>
        {% endif %}
      </div>
      <div class="col-md-9">
        <h1 class="mt-3 mt-md-0">Articles for Week {{ selected_week }}</h1> <!-- Conditional margin: add on mobile, remove on desktop -->
        {% if articles %}
          <div class="row">
            {% for article in articles %}
              <div class="col-md-6 mb-3">
                <div class="card h-100">
                  <div class="card-body d-flex flex-column justify-content-between">
                    <div>
                      <h5 class="card-title">{{ article.title }}</h5>
                      <p class="card-text"><strong>Authors:</strong> {{ article.authors }}</p>
                      <p class="card-text">{{ article.summary }}</p>
                      <h6>Key Points:</h6>
                      <ul>
                        {% for point in article.key_points %}
                          <li>{{ point }}</li>
                        {% endfor %}
                      </ul>
                    </div>
                    <div class="mt-auto text-end">
                      <a href="{{ article.link }}" target="_blank" class="btn btn-primary">Read Article</a>
                    </div>
                  </div>
                </div>
              </div>
            {% endfor %}
          </div>
        {% else %}
          <p>No articles found for week {{ selected_week }}</p>
        {% endif %}
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
